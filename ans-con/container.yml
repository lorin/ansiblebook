version: "2"
settings:
  conductor_base: debian:jessie
services:
  ac-nginx:
    from: nginx
    roles:
      - ghost-nginx
registries:
  docker:
    url: https://registry.hub.docker.com
    namespace: ansiblebook
