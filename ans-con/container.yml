version: "2"
settings:
  conductor_base: debian:jessie
services:
  ac-nginx:
    from: nginx
    roles:
      - ghost-nginx
registries:
  docker:
    namespace: ansiblebook
    url: https://index.docker.io/v1/
  quay:
    namespace: ansiblebook
    url: https://quay.io
